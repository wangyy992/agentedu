# Claude API Key(不设则需要用 --fake 走离线模式)
ANTHROPIC_API_KEY=sk-ant-...

# 可选:覆盖默认模型
# TUTOR_MODEL=claude-opus-5

# 可选:强制离线假模型,无需 API Key(CI 用的就是它)
# TUTOR_FAKE_LLM=1

# 可选:数据目录(SQLite 存放位置),默认 ./.tutor_data
# TUTOR_DATA_DIR=./.tutor_data
